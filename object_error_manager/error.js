// 1 - criar fjunção que recebe array e numero
// 2 - validações dos parametros se existem
function validaArray(arr, num) {
  if (!arr && !num) throw ReferenceError("Enviar os parâmetros");
  return;
}